# Source PDFs

Original AMS Vorarlberg course flyers (PDFs). These are the raw source
documents that get manually converted into per-course JSON files for the
Kurssuche search app — see `docs/PRD.md` for the overall workflow.

Drop the PDF files directly in this folder (via the GitHub web UI: "Add
file" → "Upload files", or `git add`).
